VERSION 2.0 del PROYECTO

Se agregaron:
    Casos de Uso:
        -AltaUsuario
        -BajaUsuario
        -ModificarUsuario
    Repositorio:
        -Usuario(Con SQLite)
        -Producto(Con SQLite)
        -Categoria(Con SQLite)
        -Trasaccion(Con SQLite)
        -Modificar(Interfaz)
    Sistema de Contraseña:
        -SistemaHashing
    Excepciones:
        -RepositoriosExcepcion
Se modifico:
    Servicio Autorizcion
    TodosLosCasosDeUso(el try catch)
    Validaciones
    Repositorios

Falta:
    -Sistema de iniciar sesion
    -La pagina web(LOZI)
