VERSION 2.0 del PROYECTO

Se agregaron:
    Casos de Uso:
        -AltaUsuario(Se verifica que no haya 2 emails iguales)
        -BajaUsuario
        -ModificarUsuario
    Repositorio:
        -Usuario(Con SQLite)
        -Producto(Con SQLite)
        -Categoria(Con SQLite)
        -Trasaccion(Con SQLite)
        -Modificar(Interfaz)
    Sistema de Contraseña:
        -SistemaHashing
    Excepciones:
        -RepositoriosExcepcion
Se modifico:
    Servicio Autorizcion
    TodosLosCasosDeUso(el try catch)
    Validaciones
    Repositorios
    el CUBajaCategoria
    try catch de CU

Falta:
    -Sistema de iniciar sesion//esta a medias
    -La pagina web(LOZI)
    -Sacar las pruebas del program
